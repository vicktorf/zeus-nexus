apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: zeus-core
  namespace: ac-agentic
  labels:
    app: zeus-core
    component: api
  annotations:
    haproxy.router.openshift.io/timeout: "300s"
    haproxy.router.openshift.io/rate-limit-connections: "true"
    haproxy.router.openshift.io/rate-limit-connections.concurrent-tcp: "100"
spec:
  host: zeus-ac-agentic.apps.prod01.fis-cloud.fpt.com
  to:
    kind: Service
    name: zeus-core
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: minio-console
  namespace: ac-agentic
  labels:
    app: minio
    component: storage
  annotations:
    haproxy.router.openshift.io/timeout: "600s"
spec:
  host: minio-ac-agentic.apps.prod01.fis-cloud.fpt.com
  to:
    kind: Service
    name: minio
    weight: 100
  port:
    targetPort: console
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: minio-api
  namespace: ac-agentic
  labels:
    app: minio
    component: storage
  annotations:
    haproxy.router.openshift.io/timeout: "600s"
spec:
  host: minio-api-ac-agentic.apps.prod01.fis-cloud.fpt.com
  to:
    kind: Service
    name: minio
    weight: 100
  port:
    targetPort: api
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None